#!/usr/bin/env node
F=(r=require)("fs");L=s=>console.log(s);(P=process).argv.slice(2).map(d=>F.readdirSync(d).map(f=>{L(`${(E=P.env).pink}${f}${R=E.reset}:`);F.readFileSync(r("path").join(d,f),"utf8").split('\n').map(l=>L(`  ${E.orange}|${R}  ${l}`));L('\n')}));

const keywords = {};
function tokenize(str) {
  const lines = str.split(/\r?\n/);
  for (let linec = 0; linec < lines.length; linec++) {
    const line = lines[linec];
    for (let charc = 0; charc < line.length; charc++) {
      const char = line[charc];
    }
  }
}
